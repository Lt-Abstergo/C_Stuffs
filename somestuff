int myStringComp(char *st1, char *st2) {
    if (strlen(st1) == strlen(st2)) {
        for (int i = 0; i < strlen(st1); ++i) {
            if (st1[i] != st2[i])
                return i;
            else
                return 0;
        }
    } else {
        return -2;
    }
}
